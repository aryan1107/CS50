#include <stdio.h>
#include <math.h>
#include <cs50.h>

int main(void)
{
    long card;
    do
    {
        // asks users for height
        card = get_long("Change owed: ");
    }
    while (card < 0);
    long temp = card;
    long calc = 0;



}